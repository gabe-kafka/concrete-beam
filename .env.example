# Set in Vercel Project Settings → Environment Variables.
# Copy this file to .env.local for local development.

# Add the prod hostname here so the API CORS allowlist accepts it.
NEXT_PUBLIC_APP_HOSTNAME=""
